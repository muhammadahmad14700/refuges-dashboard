import { combineReducers } from "redux";
import { updateAdminPasswordReducer } from "./password.reducers";

const reducer = combineReducers({
  updateAdminPasswordReducer
});

export default reducer;
