import { makeStyles } from "@material-ui/core/styles";
const useStylesBootstrap = makeStyles((theme) => ({
  arrow: {
    color: "#6D72BA",
  },
  tooltip: {
    backgroundColor: "#6D72BA",
    fontSize: "12px",
    fontFamily: "opensans-semibold",
  },
}));
export default useStylesBootstrap;